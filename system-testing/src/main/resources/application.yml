## SPRING
spring:
  #=============================
  # Data Source Config
  #=============================
  datasource:
    url: jdbc:mysql://localhost/makeitfinemysql
    username: makeitfinemysql
    password: makeitfine121
    driver-class-name: com.mysql.jdbc.Driver

    # Keep the connection alive if idle for a long time (needed in production)
    testWhileIdle: true
    validationQuery: SELECT 1